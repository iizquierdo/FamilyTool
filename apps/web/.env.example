# Frontend (Vite) env
VITE_PORT=3599
VITE_BASE_URL=/

# Backend URL — Vite dev server proxies /api and /storage to this target.
# In prod (build) the front is served same-origin and the proxy is irrelevant.
VITE_API_PROXY_TARGET=http://localhost:4099

# Optional features
# GEMINI_API_KEY=
